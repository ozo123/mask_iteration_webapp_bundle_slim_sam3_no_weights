# Web app / API
aiohttp>=3.9
openai>=1.30

# Image / data
numpy>=1.24
Pillow>=10.0
pycocotools>=2.0

# SAM3 common dependencies
fvcore==0.1.5.post20221221
hydra-core>=1.1
omegaconf>=2.1
einops>=0.6
wget>=3.0

# Note:
# torch is NOT pinned here on purpose.
# Install the correct torch build manually for your platform:
# - NVIDIA CUDA
# - Apple Silicon / MPS
# - CPU-only
